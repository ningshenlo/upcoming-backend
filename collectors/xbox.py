from __future__ import annotations

from core.models import CollectorResult


def collect(limit: int, user_agent: str) -> CollectorResult:
    raise NotImplementedError("Xbox/Microsoft official collector is planned after Steam/Nintendo.")
